hotkey: <cmd>+<shift>+i
whisper_model: large-v3-turbo  # tiny, base, small, medium, large-v3, large-v3-turbo
ollama_model: qwen3:8b     # any model pulled in ollama
ollama_mode: external      # "external" (system Ollama) or "bundled" (packaged binary)
language:                   # auto-detect if empty, or set: en, hi, ml, fr, etc.
